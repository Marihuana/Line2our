<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="item"
            type="com.four.hackerton.line2our.model.network.model.PlaceVO" />
        <variable
            name="position"
            type="String" />

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="91dp"
        android:background="#f8f8f8"
        >

        <TextView
            android:id="@+id/tvPosition"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_marginTop="15dp"
            android:layout_marginStart="21dp"
            android:text="@{position}"
            android:textColor="#464646"
            android:textSize="20dp"
            android:textStyle="bold"
            />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toRightOf="@id/tvPosition"
            app:layout_constraintRight_toLeftOf="@id/loButtons"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:orientation="vertical"
            android:layout_marginTop="15dp"
            android:layout_marginBottom="15dp"
            android:layout_marginStart="20dp"
            >

            <TextView
                android:id="@+id/tvPlaceName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{item.name}"
                android:textSize="20dp"
                android:textColor="#464646"
                android:textStyle="bold"
                />

            <TextView
                android:id="@+id/tvPlaceAddress"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="10.5dp"
                android:textColor="#464646"
                android:text="@{item.address}"
                />
            
            <TextView
                android:id="@+id/tvPhone"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{item.phone}"
                android:textColor="#4a65ff"
                />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/loButtons"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="21dp"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:orientation="vertical">

            <com.four.hackerton.line2our.feature.common.CustomCheckButton
                android:id="@+id/btnRecommend"
                android:layout_width="75dp"
                android:layout_height="30dp"
                app:isLike="true"
                android:elevation="6dp"
                />

            <com.four.hackerton.line2our.feature.common.CustomCheckButton
                android:id="@+id/btnBookMark"
                android:layout_width="75dp"
                android:layout_height="30dp"
                android:elevation="6dp"
                app:isLike="false"
                android:layout_marginTop="5dp"
                />

        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

